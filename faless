#!/bin/bash
# (c) Roel Kluin, 2012, NKI

F="$1"

if [ ! -e "$1" ]; then
   ./facolor.pl "$@" 2>&1 | less -R
else
	shift;
	if [[ "$F" =~ \.gz$ ]]; then
		zcat "$F" | ./facolor.pl "$@" 2>&1 | less -R
	else
		cat "$F" | ./facolor.pl "$@" 2>&1 | less -R
	fi
fi
